FROM denoland/deno:latest
RUN apt-get update && apt-get install -y git && rm -rf /var/lib/apt/lists/*
WORKDIR /app
COPY . .
# Deno will install dependencies when running tests
CMD ["deno", "task", "test"]
